# os-privilege-separation-and-sandbox
C implementations of OS-level security: a privilege-separated password validation service (fork/execve, setresuid, UNIX domain sockets, SO_PEERCRED) and a pthreads-based sandbox for supervising and terminating untrusted binaries via /proc monitoring and signals.
